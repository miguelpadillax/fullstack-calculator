spring:
  application:
    name: calculator
  jackson:
    generator:
      write-bigdecimal-as-plain: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

calculator:
  cors:
    allowed-origins: http://localhost:5173
